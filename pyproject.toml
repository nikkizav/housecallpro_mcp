[project]
name = "housecallpro-mcp"
version = "0.1.0"
description = "Local MCP server connecting Claude Desktop to Housecall Pro"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]
